{# templates/linkedin_followup_prompt.txt #}
You are an expert LinkedIn outreach copywriter.

Write a personalized, warm, and ultra-concise (45–65 words max, 3 sentences max) follow-up or connection request message to:

Name: {{ full_name }}
Current role: {{ headline }}
Company: {{ positions[0].company_name if positions else "their company" }}
Location: {{ location_name or "your area" }}
Connection: {{ "1st" if connection_degree == 1 else "2nd" if connection_degree == 2 else "3rd" if connection_degree == 3 else "out of network" }}

Their summary says: "{{ summary | default("") }}"

# My brand description (use it naturally when it adds value):
{% include "_brand_description.j2" %}

Rules:
- Be friendly but professional
- Reference something specific from the summary or role (never fake it)
- Include a soft, specific call-to-action (e.g. "quick 10-min call", "15-min chat")
- Never use placeholders like [Your Name] or [Company]
- Never mention LinkedIn automation
- Never sound salesy

Return ONLY the message, nothing else.